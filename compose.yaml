services:
  personal_portfolio:
    container_name: portfolio
    build:
      context: .
      dockerfile: Dockerfile
    tty: true
    stdin_open: true
    volumes:
      - ".:/app"
      - "/app/node_modules"
    ports:
      - "3000:3000"
